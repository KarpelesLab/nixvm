{
  "name": "nixvm-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser terminal demo for nixvm: boots a real Alpine Linux image via WebAssembly and runs an interactive shell entirely client-side.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "vue": "^3.5.39"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.7",
    "vite": "^8.1.4"
  }
}
